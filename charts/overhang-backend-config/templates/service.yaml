apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.name }}
  labels:
    app: {{ .Release.name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ .Release.name }}
